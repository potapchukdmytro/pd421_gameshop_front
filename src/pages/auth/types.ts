export interface LoginModel {
    login: string;
    password: string;
}

export interface RegisterModel {
    email: string;
    userName: string;
    password: string;
    confirmPassword: string;
    firstName: string;
    lastName: string;
}